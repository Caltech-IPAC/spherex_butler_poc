description: ExamplePipeline
tasks:
  subtract:
    class: spherex.tasks.SubtractTask
    config:
      connections.inputImage: 'rawexp'
      connections.subtractImage: 'dark'
      connections.outputImage: 'postDark'